const localhost = "http://localhost:8000"
const withAuth = {withCredentials:true}

// The functionality of this file has been replaced by axios. Now this is used for keeping setings
module.exports = {localhost,withAuth };
